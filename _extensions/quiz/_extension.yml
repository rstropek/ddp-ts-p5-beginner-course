title: Quiz
author: CS Basics Course
version: 1.0.0
quarto-required: ">=1.4.0"
# When the project has this extension installed (i.e. the `_extensions/quiz/`
# folder exists), Quarto makes the `{{< quiz ... >}}` shortcode available in
# any .qmd.
contributes:
  shortcodes:
    - quiz.lua
